{
  "$schema": "../../../schemas/type.schema.json",
  "id": "backend-pyproject",
  "description": "pyproject.toml for the FastAPI backend",
  "parameters": {
    "title": { "type": "string", "default": "FastAPI Application" },
    "version": { "type": "string", "default": "0.1.0" }
  },
  "template": "templates/backend/pyproject.toml.j2",
  "targetType": "python-module",
  "outputPattern": "generated-project/apps/api/pyproject.toml"
}
