from typing import Optional
from uuid import UUID


class {{ name }}Service:
    def __init__(self) -> None:
        self._items: dict[UUID, {{ entity_name }}] = {}

    async def create(self, data: {{ entity_name }}Base) -> {{ entity_name }}:
        entity = {{ entity_name }}(**data.model_dump())
        self._items[entity.id] = entity
        return entity

    async def get(self, entity_id: UUID) -> Optional[{{ entity_name }}]:
        return self._items.get(entity_id)

    async def list(self) -> list[{{ entity_name }}]:
        return list(self._items.values())

    async def delete(self, entity_id: UUID) -> bool:
        return self._items.pop(entity_id, None) is not None
