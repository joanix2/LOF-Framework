import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import type { {{ id | pascal_case }}, {{ id | pascal_case }}Create, {{ id | pascal_case }}Update, {{ id | pascal_case }}Page } from '../types/{{ id }}';

const API_BASE = import.meta.env.VITE_API_URL || 'http://localhost:8000/api/v1';

export function use{{ id | pascal_case }}List(page = 1, pageSize = 20, search?: string) {
  return useQuery<{{ id | pascal_case }}Page>({
    queryKey: ['{{ id }}', 'list', page, pageSize, search],
    queryFn: async () => {
      const params = new URLSearchParams({ page: String(page), page_size: String(pageSize) });
      if (search) params.set('search', search);
      const res = await fetch(`${API_BASE}/{{ route }}?${params}`);
      if (!res.ok) throw new Error('Failed to fetch {{ pluralName | lower }}');
      return res.json();
    },
  });
}

export function use{{ id | pascal_case }}(id: string) {
  return useQuery<{{ id | pascal_case }}>({
    queryKey: ['{{ id }}', id],
    queryFn: async () => {
      const res = await fetch(`${API_BASE}/{{ route }}/${id}`);
      if (!res.ok) throw new Error('Failed to fetch {{ name | lower }}');
      return res.json();
    },
    enabled: !!id,
  });
}

export function useCreate{{ id | pascal_case }}() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (data: {{ id | pascal_case }}Create) => {
      const res = await fetch(`${API_BASE}/{{ route }}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });
      if (!res.ok) throw new Error('Failed to create {{ name | lower }}');
      return res.json();
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['{{ id }}', 'list'] });
    },
  });
}

export function useUpdate{{ id | pascal_case }}() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({ id, data }: { id: string; data: {{ id | pascal_case }}Update }) => {
      const res = await fetch(`${API_BASE}/{{ route }}/${id}`, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });
      if (!res.ok) throw new Error('Failed to update {{ name | lower }}');
      return res.json();
    },
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['{{ id }}', 'list'] });
      queryClient.invalidateQueries({ queryKey: ['{{ id }}', variables.id] });
    },
  });
}

export function useDelete{{ id | pascal_case }}() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (id: string) => {
      const res = await fetch(`${API_BASE}/{{ route }}/${id}`, { method: 'DELETE' });
      if (!res.ok) throw new Error('Failed to delete {{ name | lower }}');
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['{{ id }}', 'list'] });
    },
  });
}
