{
  "name": "language-oriented-framework",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "format": "prettier --write '**/*.{json,md,yaml}'",
    "lint": "prettier --check '**/*.{json,md,yaml}'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "vitest": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
